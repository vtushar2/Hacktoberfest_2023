from heapq import heappop, heappush, heapify

# MIN HEAP BY DEAFULT
# Heapify array
heapify(arr)

#len(arr) works as usual
heappush(arr, newNumber)
poppedElement = heappop(arr)

heapify([-i for i in nums])  # Max Heap